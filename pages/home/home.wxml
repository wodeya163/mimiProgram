<view>
	<view class="view-image">
		<image mode="aspectFit" src="{{avatarUrl}}" />
	</view>
	<view class="view-text">
		<text>{{nickName}}</text>
	</view>
	<button open-type="getUserInfo" bindgetuserinfo="getUserInfo" size="mini" type="primary">授权
	</button>
</view>